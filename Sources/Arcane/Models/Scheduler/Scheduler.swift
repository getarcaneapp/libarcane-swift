import Foundation

// The Go `types/scheduler` package only defines Go interfaces (`Job`,
// `ConditionalJob`) for backend wiring; there are no HTTP-facing DTOs in this
// package. This file is intentionally empty so the Models/Scheduler directory
// exists for parity with the Go layout.
